/// <reference types="vite/client" />

/** Cloud API origin baked in at build time. Override with CLOUD_API_URL. */
declare const __CLOUD_API_URL__: string;

/** Cloud web origin baked in at build time. Override with CLOUD_WEB_URL. */
declare const __CLOUD_WEB_URL__: string;
